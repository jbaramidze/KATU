/* fread example: read an entire file */
#include <stdio.h>
#include <stdlib.h>
#include "dr_annotations_zhani.h"
  

char b[8];

int main () {

  nshrtaint((long long int) &b, 1);
  nshrtaint((long long int) &b, 1);
  nshrtaint((long long int) &b, 1);
  nshrtaint((long long int) &b, 1);
  nshrtaint((long long int) &b, 1);
  nshrtaint((long long int) &b, 1);

  return 0;
}